# Local dev: point at your local Flask backend
VITE_API_URL=http://localhost:5000

# Once deployed, set VITE_API_URL in Vercel's dashboard (Project Settings -> Environment Variables)
# to your Render backend URL instead, e.g.:
# VITE_API_URL=https://level-up-interview-api.onrender.com
